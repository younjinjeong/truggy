#include <cstdio>

namespace truggy {

// TODO(epic4): port MPPICosts from AutoRally
// ref: costs.cu — 5-term cost function (speed, track, stabilizing, control, crash)

void costs_init() {
    fprintf(stderr, "[costs] init (stub)\n");
}

} // namespace truggy
